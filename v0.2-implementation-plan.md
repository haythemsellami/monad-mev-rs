# monad-mev-rs v0.2 Implementation Plan

Status: Complete
Status date: 2026-05-25

## 1. Goal

v0.2 continues the v0.1 design. `monad-mev-rs` remains an Execution Events-first
MEV framework, not a generic bot engine where Execution Events are just another
optional source.

The v0.2 implementation goal is:

> Let a developer capture the Execution Events relevant to DeFi protocol X,
> attach local or imported strategy Y, maintain protocol state, optionally
> simulate and risk-check transaction candidates, and execute or record outcomes
> without touching raw event-ring APIs.

v0.2 is not a refactor reset. Every work package should extend v0.1's existing
public concepts where practical:

- `StreamItem<T>`
- `EventEnvelope<T>`
- `ChainEvent`
- replay and fixture APIs
- live observe APIs
- strategy and executor traits
- recording and dry-run executors

## 2. v0.2 Delivery Shape

v0.2 should be delivered in staged capability bands:

| Band | Purpose | Production submission |
|---|---|---|
| v0.2-A | Execution Events stream and generic capture | No |
| v0.2-B | Strategy packages and engine runtime | No |
| v0.2-C | State stores, adapters, and read-only opportunities | No |
| v0.2-D | Simulation and state hydration | No |
| v0.2-E | Risk and explicit execution plans | No by default |
| v0.2-F | Production executors behind opt-in features/config | Yes, explicit only |
| v0.2-G | Observability, persistence, docs, release hardening | Depends on config |

The first concrete v0.2 target is a protocol-agnostic end-to-end lifecycle
harness. It should run over fixtures and snapshots before any production
submission path exists.

## 3. Engineering Rules

- Keep Execution Events as the source of truth for on-chain activity.
- Treat RPC, timers, config reloads, and metadata fetches as auxiliary inputs.
- Preserve replay/live semantic parity.
- Do not introduce production transaction submission until simulation, risk,
  nonce, audit, and fake-transport tests exist.
- Default examples must stay recording or dry-run only.
- Do not require users to understand raw descriptors or SDK payload lifetimes
  for ordinary protocol/strategy development.
- Add abstractions only where v0.1 APIs become insufficient for real event
  capture or strategy execution.
- Keep protocol math, protocol-specific state machines, and strategy families
  outside the core framework unless they are only test fixtures or examples.
- Keep production behavior explicit and auditable.

## 4. Milestones

### M1: Execution Events Stream And Capture

Goal: make v0.1 event sources look like one reusable stream contract and let users
capture relevant subsets for any protocol or application domain.

Work packages:

- WP-01: shared Execution Events stream abstraction.
- WP-02: protocol capture filters.
- WP-03: replay/live parity checks.

Exit criteria:

- Fixture, snapshot, and live observe paths can expose the same stream item
  shape.
- A capture filter can select events by address, topic, kind, transaction, and
  commit-state requirement.
- Capture output is typed enough for strategies to consume without raw
  descriptor access.

### M2: Strategy Packages And Engine Runtime

Goal: support local/imported strategies and route captured protocol events into
them.

Work packages:

- WP-04: strategy package conventions.
- WP-05: `monad-mev-engine` crate.
- WP-06: strategy subscriptions and routing.
- WP-07: adapters for typed events/actions.

Exit criteria:

- One Execution Events stream can feed multiple protocol adapters and
  strategies.
- Strategies can be local crates or generated/imported templates.
- Strategy packages can expose metadata, subscriptions, and tests.

### M3: State Stores And Generic Adapters

Goal: maintain user-defined state from captured Execution Events without baking
any protocol family into core.

Work packages:

- WP-08: `monad-mev-store` crate.
- WP-09: generic state update and projection primitives.
- WP-10: domain adapter interfaces.
- WP-11: generic monitor strategy template.
- WP-12: end-to-end fixture domain adapter.
- WP-13: external package interface.

Exit criteria:

- State can be updated from replayed events deterministically.
- State views include commit-state context.
- Adapter contracts are protocol-agnostic.
- The lifecycle fixture proves that external domain logic can update state and
  emit opportunities without core DEX, lending, or oracle concepts.

### M4: End-To-End Strategy Lifecycle Harness

Goal: prove the full framework lifecycle without hardcoding a real protocol
family into the core crates.

Work packages:

- WP-14: generic opportunity and signal model.
- WP-15: read-only lifecycle detector strategy.
- WP-16: lifecycle fixtures and golden output.

Exit criteria:

- Non-matching inputs are ignored.
- Matching state emits deterministic opportunity records.
- Stale or incomplete state rejects opportunities.
- Opportunity records include source event refs and state version.

### M5: Simulation

Goal: turn opportunities into transaction candidates and simulate them.

Work packages:

- WP-17: `monad-mev-sim` request/result API.
- WP-18: RPC-backed lazy state hydration.
- WP-19: Monad EVM simulation integration.
- WP-20: simulation golden tests.

Exit criteria:

- Simulation records success/revert, gas, deltas, expected profit, and state
  freshness.
- Missing state reads are auditable.
- Failed or stale simulations prevent execution by default.

### M6: Risk And Execution Plans

Goal: make execution decisions explicit, explainable, and safe by default.

Work packages:

- WP-21: `monad-mev-risk` crate.
- WP-22: execution plan and audit log model.
- WP-23: risk policy engine.
- WP-24: nonce and replacement policy.

Exit criteria:

- Risk approvals/rejections are structured and explainable.
- Execution plans cannot be built without simulation and risk output unless an
  explicit unsafe mode is enabled.
- Audit logs are deterministic JSONL.

### M7: Explicit Production Executors

Goal: add submission interfaces behind opt-in features and config.

Work packages:

- WP-25: production executor interfaces.
- WP-26: fake transport and executor harness.
- WP-27: RPC submit executor.
- WP-28: relay/bundle executor interfaces if Monad exposes a stable path.
- WP-29: simulated execution template.
- WP-30: risk-checked submit path.

Exit criteria:

- Production submission is impossible by default.
- Every production executor has fake transport tests.
- Private keys are never embedded in examples.
- Submitted receipts are visibly distinct from dry-run and simulation receipts.

### M8: Operations And Release

Goal: make v0.2 observable, testable, and usable outside local development.

Work packages:

- WP-31: metrics and tracing.
- WP-32: persistent state backend.
- WP-33: snapshot/live parity test suite.
- WP-34: production runbook.
- WP-35: release hardening.

Exit criteria:

- Metrics cover ingestion, strategy, simulation, risk, execution, backpressure,
  gaps, and drops.
- Persistent state has deterministic restore tests.
- Docs explain replay, dry-run, simulation, and production modes.

## 5. Work Packages

### WP-01: Shared Execution Events Stream Abstraction

Status: [x]

Goal: expose fixture, snapshot, and live observe sources through one
Execution Events-first stream contract.

Implementation tasks:

- [x] Define `ExecutionEventStream<T>` or equivalent wrapper around
  `StreamItem<T>`.
- [x] Add source metadata carrying fixture/snapshot/live source kind.
- [x] Preserve v0.1 `StreamItem` variants.
- [x] Preserve `EventEnvelope` metadata.
- [x] Add stream construction from fixture documents.
- [x] Add stream construction from snapshot readers.
- [x] Add live observe stream constructor behind `live`.
- [x] Keep source-specific raw APIs available for advanced users.

Testing tasks:

- [x] Fixture stream parity test.
- [x] Snapshot stream parity test using synthetic snapshot data.
- [x] Live observe stream unit test behind `live`.
- [x] Source metadata serialization test.
- [x] Gap and payload-expiration propagation test.

Acceptance:

```bash
cargo test -p monad-mev-events execution_stream
cargo test -p monad-mev-events --features live live
```

### WP-02: Protocol Capture Filters

Status: [x]

Goal: let users capture events for protocol X without raw event-ring or ABI
plumbing.

Implementation tasks:

- [x] Define `ProtocolCapture`.
- [x] Define capture criteria: addresses, topic0, event kinds, transaction
  index, block range, commit-state minimum.
- [x] Define capture output carrying `EventEnvelope<ChainEvent>`.
- [x] Add reusable `AddressSet` and topic filters.
- [x] Add ABI-backed event capture helper.
- [x] Add reusable capture presets that external packages can compose.
- [x] Add explain output for why an event matched or was ignored.

Testing tasks:

- [x] Address filter test.
- [x] Topic filter test.
- [x] Combined filter test.
- [x] Commit-state minimum test.
- [x] ABI-backed filter test.
- [x] Determinism test over fixture streams.

Acceptance:

```bash
cargo test -p monad-mev-events protocol_capture
```

### WP-03: Replay/Live Parity Checks

Status: [x]

Goal: ensure replay, snapshot, and live observe paths use the same event-time,
filtering, and routing semantics.

Implementation tasks:

- [x] Define framework event time.
- [x] Normalize fixture event time to the same representation.
- [x] Normalize snapshot record timestamps.
- [x] Normalize live record timestamps.
- [x] Ensure filtering happens at the same semantic point for replay and live.
- [x] Add parity report output.

Testing tasks:

- [x] Fixture vs snapshot-equivalent parity test.
- [x] Multi-stream interleaving test.
- [x] Filter-before-time and time-before-filter regression test.
- [x] Golden parity report.

Acceptance:

```bash
cargo test --workspace replay_live_parity
```

### WP-04: Strategy Package Conventions

Status: [x]

Goal: let developers attach local or imported strategies to captured protocol
events.

Implementation tasks:

- [x] Define strategy package manifest fields.
- [x] Define strategy metadata: name, version, event subscriptions, action
  output type, risk requirements.
- [x] Extend `strategy new` for v0.2 strategy packages.
- [x] Add generated tests using protocol capture fixtures.
- [x] Add examples for local crate import.
- [x] Document how third-party strategy crates integrate.

Testing tasks:

- [x] Generated package compiles.
- [x] Generated package test passes.
- [x] Imported package fixture test.
- [x] Manifest validation test.

Acceptance:

```bash
cargo test -p monad-mev-cli strategy_package
```

### WP-05: `monad-mev-engine`

Status: [x]

Goal: add an engine crate that routes Execution Events streams into strategies
and executors.

Implementation tasks:

- [x] Add `crates/monad-mev-engine`.
- [x] Define engine config.
- [x] Define collector/source registration for Execution Events streams.
- [x] Define auxiliary collector registration for RPC/timers/config.
- [x] Define strategy registration.
- [x] Define executor registration.
- [x] Add bounded channel config.
- [x] Add shutdown handling.
- [x] Add sync and async compatibility decision.
- [x] Add recording executor integration.

Testing tasks:

- [x] Single stream single strategy test.
- [x] Single stream multiple strategy test.
- [x] Multiple protocol adapters test.
- [x] Bounded channel backpressure test.
- [x] Strategy error shutdown test.
- [x] Source-ended shutdown test.

Acceptance:

```bash
cargo test -p monad-mev-engine
```

### WP-06: Strategy Subscriptions And Routing

Status: [x]

Goal: route events only to strategies that requested them.

Implementation tasks:

- [x] Define `StrategySubscription`.
- [x] Support event kind filters.
- [x] Support address/topic filters.
- [x] Support protocol adapter output type.
- [x] Support minimum commit-state requirement.
- [x] Add routing explain/debug mode.

Testing tasks:

- [x] Route matching event test.
- [x] Drop non-matching event test.
- [x] Multiple subscribers test.
- [x] Commit-state routing test.
- [x] Routing explain test.

Acceptance:

```bash
cargo test -p monad-mev-engine routing
```

### WP-07: Typed Event And Action Adapters

Status: [x]

Goal: let strategies use local event/action types while still plugging into the
shared engine.

Implementation tasks:

- [x] Define event adapter trait.
- [x] Define action adapter trait.
- [x] Add map/filter adapter helpers.
- [x] Add boxed adapter support.
- [x] Add adapter error type.
- [x] Add examples adapted from Artemis' wrapper pattern.

Testing tasks:

- [x] Event map test.
- [x] Event filter test.
- [x] Action map test.
- [x] Adapter error propagation test.
- [x] Strategy with local action type test.

Acceptance:

```bash
cargo test -p monad-mev-engine adapter
```

### WP-08: `monad-mev-store`

Status: [x]

Goal: provide commit-state-aware state views for protocol adapters and
strategies.

Implementation tasks:

- [x] Add `crates/monad-mev-store`.
- [x] Define `StateVersion`.
- [x] Define `CommitScopedView`.
- [x] Add in-memory store.
- [x] Add snapshot/restore for tests.
- [x] Add rollback handling model.
- [x] Add state update audit records.

Testing tasks:

- [x] Insert/update/read test.
- [x] Commit-state scoped read test.
- [x] Rollback/regression test.
- [x] Snapshot/restore determinism test.
- [x] Audit record test.

Acceptance:

```bash
cargo test -p monad-mev-store
```

### WP-09: Generic State Update And Projection Primitives

Status: [x]

Goal: support adapters that need deterministic state mutations and derived
views without tying core to one protocol family.

Implementation tasks:

- [x] Define typed state update envelope.
- [x] Define projection trait.
- [x] Add deterministic update ordering.
- [x] Add source event reference model.
- [x] Add fixture projection helper.

Testing tasks:

- [x] Projection update test.
- [x] Deterministic ordering test.
- [x] Source event reference test.
- [x] Unknown key fallback test.

Acceptance:

```bash
cargo test -p monad-mev-store projection
```

### WP-10: Domain Adapter Interfaces

Status: [x]

Goal: define how external crates translate framework events into domain events,
state updates, opportunities, and actions.

Implementation tasks:

- [x] Add adapter metadata model.
- [x] Define event adapter trait.
- [x] Define state adapter trait.
- [x] Define opportunity detector trait.
- [x] Define adapter capability flags.
- [x] Add boxed adapter support.
- [x] Add adapter error type.

Testing tasks:

- [x] Event adapter test.
- [x] State adapter test.
- [x] Opportunity detector test.
- [x] Boxed adapter dispatch test.
- [x] Capability serialization test.

Acceptance:

```bash
cargo test -p monad-mev-engine adapter
```

### WP-11: Generic Monitor Strategy Template

Status: [x]

Goal: provide a protocol-agnostic strategy template over captured events and
adapter output.

Implementation tasks:

- [x] Add generic monitor strategy package.
- [x] Subscribe to configured capture filters.
- [x] Read adapter-produced opportunities.
- [x] Emit structured records.
- [x] Add CLI scaffold option.

Testing tasks:

- [x] Fixture replay monitor test.
- [x] Golden JSONL output test.
- [x] Missing adapter state warning test.

Acceptance:

```bash
cargo test -p monad-mev-engine monitor_template
```

### WP-12: End-To-End Fixture Domain Adapter

Status: [x]

Goal: provide a fake but realistic domain adapter used only to prove the
framework lifecycle.

Implementation tasks:

- [x] Define fixture domain event.
- [x] Define fixture state model.
- [x] Add adapter trait implementation for fixture events.
- [x] Add opportunity condition.
- [x] Add incomplete/stale state handling.
- [x] Add replay fixture implementation.

Testing tasks:

- [x] Fixture state validation.
- [x] State update test.
- [x] Stale/incomplete state behavior.
- [x] Fixture adapter replay test.

Acceptance:

```bash
cargo test -p monad-mev-engine lifecycle_fixture
```

### WP-13: External Package Interface

Status: [x]

Goal: make third-party protocol and strategy packages easy to plug into the
engine.

Implementation tasks:

- [x] Define package manifest model.
- [x] Define package capability metadata.
- [x] Define subscription declarations.
- [x] Define strategy factory trait.
- [x] Add manifest validation.

Testing tasks:

- [x] Manifest validation test.
- [x] Subscription declaration test.
- [x] Package metadata serialization test.
- [x] Missing capability test.

Acceptance:

```bash
cargo test -p monad-mev-engine package_manifest
```

### WP-14: Generic Opportunity And Signal Model

Status: [x]

Goal: define typed opportunity records independent of any protocol family or
execution backend.

Implementation tasks:

- [x] Define `Opportunity`.
- [x] Define `Signal`.
- [x] Include source event refs.
- [x] Include state version.
- [x] Include rejection reasons.
- [x] Add serialization.

Testing tasks:

- [x] Opportunity serialization test.
- [x] Rejection reason test.
- [x] Source event refs test.

Acceptance:

```bash
cargo test -p monad-mev-engine opportunity
```

### WP-15: Read-Only Lifecycle Detector Strategy

Status: [x]

Goal: detect opportunities from adapter state without building or submitting
transactions.

Implementation tasks:

- [x] Add read-only lifecycle strategy.
- [x] Subscribe to fixture domain captures.
- [x] Read state projection.
- [x] Reject non-matching state.
- [x] Reject stale state.
- [x] Emit structured opportunity records.
- [x] Add CLI scaffold/template hook.

Testing tasks:

- [x] Non-matching state ignored.
- [x] Matching state emits opportunity.
- [x] Stale state rejects opportunity.
- [x] Incomplete state rejects opportunity.
- [x] Golden JSONL output.

Acceptance:

```bash
cargo test -p monad-mev-engine lifecycle_read_only
```

### WP-16: Lifecycle Fixtures And Goldens

Status: [x]

Goal: provide deterministic fixture coverage for end-to-end lifecycle tests.

Implementation tasks:

- [x] Add source event fixture.
- [x] Add state update fixture.
- [x] Add opportunity fixture.
- [x] Add matching and non-matching cases.
- [x] Add stale state case.
- [x] Add golden opportunity JSONL.

Testing tasks:

- [x] Fixture loader test.
- [x] Golden output determinism test.
- [x] Replay through engine test.

Acceptance:

```bash
cargo test --workspace lifecycle_fixture
cargo test --workspace lifecycle_golden
```

### WP-17: `monad-mev-sim` Request/Result API

Status: [x]

Goal: define simulation contracts before selecting the final EVM backend.

Implementation tasks:

- [x] Add `crates/monad-mev-sim`.
- [x] Define `SimulationRequest`.
- [x] Define `SimulationResult`.
- [x] Define `SimulationContext`.
- [x] Define placement model.
- [x] Define balance delta model.
- [x] Define revert diagnostics.
- [x] Add JSON serialization.

Testing tasks:

- [x] Request serialization.
- [x] Result serialization.
- [x] Placement model validation.
- [x] Revert diagnostic formatting.

Acceptance:

```bash
cargo test -p monad-mev-sim
```

### WP-18: RPC-Backed Lazy State Hydration

Status: [x]

Goal: fetch missing state for simulation with auditable reads.

Implementation tasks:

- [x] Define state provider trait.
- [x] Add RPC provider interface.
- [x] Add cached provider wrapper.
- [x] Record every remote state read.
- [x] Add timeout and retry policy.
- [x] Add fake provider.

Testing tasks:

- [x] Fake provider happy path.
- [x] Cache hit/miss test.
- [x] Missing state error test.
- [x] Audit remote reads test.
- [x] Timeout policy test.

Acceptance:

```bash
cargo test -p monad-mev-sim state_hydration
```

### WP-19: Monad EVM Simulation Integration

Status: [x]

Goal: execute transaction candidates against Monad-compatible EVM semantics.

Implementation tasks:

- [x] Select backend crate/version.
- [x] Add feature-gated backend dependency.
- [x] Map `TxCandidate` into backend transaction.
- [x] Map state provider into backend DB.
- [x] Map result into `SimulationResult`.
- [x] Add gas and balance delta extraction.
- [x] Add revert diagnostics.

Testing tasks:

- [x] Simple successful call simulation.
- [x] Revert simulation.
- [x] Missing state hydration test.
- [x] Balance delta test.
- [x] Feature-gated compile test.

Acceptance:

```bash
cargo test -p monad-mev-sim --features monad-evm
```

### WP-20: Simulation Golden Tests

Status: [x]

Goal: make simulation deterministic enough to trust and review.

Implementation tasks:

- [x] Add simulation fixture format.
- [x] Add golden success result.
- [x] Add golden revert result.
- [x] Add stale state fixture.
- [x] Add profit calculation fixture.

Testing tasks:

- [x] Golden success result test.
- [x] Golden revert result test.
- [x] Profit determinism test.
- [x] Stale state rejection test.

Acceptance:

```bash
cargo test -p monad-mev-sim golden
```

### WP-21: `monad-mev-risk`

Status: [x]

Goal: add mandatory risk decisions before production execution.

Implementation tasks:

- [x] Add `crates/monad-mev-risk`.
- [x] Define `RiskPolicy`.
- [x] Define `RiskDecision`.
- [x] Define `RiskRejection`.
- [x] Add max gas, max notional, min profit, max loss.
- [x] Add allowed target and selector policies.
- [x] Add external-data freshness and simulation freshness checks.
- [x] Add circuit breaker.

Testing tasks:

- [x] Max gas reject.
- [x] Min profit reject.
- [x] Stale external-data reject.
- [x] Stale simulation reject.
- [x] Allowed target/selector reject.
- [x] Circuit breaker test.

Acceptance:

```bash
cargo test -p monad-mev-risk
```

### WP-22: Execution Plan And Audit Log

Status: [x]

Goal: create a risk-approved execution object and durable audit records.

Implementation tasks:

- [x] Define `TxCandidate`.
- [x] Define `ExecutionPlan`.
- [x] Define `AuditRecord`.
- [x] Include source event refs.
- [x] Include state version.
- [x] Include simulation and risk output.
- [x] Add JSONL writer.
- [x] Add deterministic IDs.

Testing tasks:

- [x] Execution plan serialization.
- [x] Audit JSONL determinism.
- [x] Missing simulation cannot build default plan.
- [x] Missing risk approval cannot build default plan.

Acceptance:

```bash
cargo test -p monad-mev-core execution_plan
cargo test -p monad-mev-core audit
```

### WP-23: Risk Policy Engine

Status: [x]

Goal: evaluate execution plans against configured risk policies.

Implementation tasks:

- [x] Add policy config parser.
- [x] Add policy composition.
- [x] Add rejection explanation output.
- [x] Add risk audit records.
- [x] Add dry-run risk report CLI.

Testing tasks:

- [x] Policy config test.
- [x] Multiple rejection aggregation test.
- [x] Risk audit record test.
- [x] CLI dry-run report test.

Acceptance:

```bash
cargo test -p monad-mev-risk policy_engine
```

### WP-24: Nonce And Replacement Policy

Status: [x]

Goal: define safe transaction sequencing behavior.

Implementation tasks:

- [x] Define nonce provider trait.
- [x] Define pending nonce state.
- [x] Define replacement policy.
- [x] Define max pending tx policy.
- [x] Add fake provider.
- [x] Add audit records.

Testing tasks:

- [x] Fresh nonce test.
- [x] Pending nonce test.
- [x] Replacement allowed test.
- [x] Replacement rejected test.
- [x] Max pending reject test.

Acceptance:

```bash
cargo test -p monad-mev-exec nonce
```

### WP-25: Production Executor Interfaces

Status: [x]

Goal: define production submission interfaces without enabling default
submission.

Implementation tasks:

- [x] Add `crates/monad-mev-exec`.
- [x] Define signer trait.
- [x] Define submit transport trait.
- [x] Define submit receipt.
- [x] Define production mode config.
- [x] Require explicit production enable flag.
- [x] Redact signer config in debug output.

Testing tasks:

- [x] Production disabled by default.
- [x] Signer redaction test.
- [x] Submit receipt serialization.
- [x] Missing explicit config rejects submission.

Acceptance:

```bash
cargo test -p monad-mev-exec production_interface
```

### WP-26: Fake Transport And Executor Harness

Status: [x]

Goal: test production executor logic without real network submission.

Implementation tasks:

- [x] Add fake submit transport.
- [x] Add fake signer.
- [x] Add response scripting.
- [x] Add latency/error injection.
- [x] Add receipt capture.

Testing tasks:

- [x] Successful fake submit.
- [x] Failed fake submit.
- [x] Retry policy test.
- [x] Receipt audit test.

Acceptance:

```bash
cargo test -p monad-mev-exec fake_transport
```

### WP-27: RPC Submit Executor

Status: [x]

Goal: submit approved execution plans over RPC behind explicit config.

Implementation tasks:

- [x] Add RPC transport.
- [x] Wire signer.
- [x] Wire nonce manager.
- [x] Wire replacement policy.
- [x] Wire risk-approved execution plan input.
- [x] Add feature gate if needed.
- [x] Add docs warning.

Testing tasks:

- [x] Fake RPC submit test.
- [x] Nonce use test.
- [x] Replacement test.
- [x] Runtime config reject test.
- [x] No private key in logs test.

Acceptance:

```bash
cargo test -p monad-mev-exec rpc_submit
```

### WP-28: Relay/Bundle Executor Interfaces

Status: [x]

Goal: prepare extension points for private relay or bundle submission if Monad
standardizes a path.

Implementation tasks:

- [x] Define relay submit trait.
- [x] Define bundle request/receipt types.
- [x] Define relay capability discovery.
- [x] Add placeholder docs for unsupported mode.

Testing tasks:

- [x] Type serialization.
- [x] Unsupported relay returns clear error.
- [x] Fake relay submit test.

Acceptance:

```bash
cargo test -p monad-mev-exec relay
```

### WP-29: Simulated Execution Template

Status: [x]

Goal: extend read-only opportunities into generic simulated transaction
candidates.

Implementation tasks:

- [x] Add transaction candidate builder trait.
- [x] Add simulation integration.
- [x] Add value estimate hook.
- [x] Add stale state rejection.
- [x] Add simulated candidate audit record.

Testing tasks:

- [x] Candidate builder test.
- [x] Successful simulation test.
- [x] Revert rejection test.
- [x] Value estimate test.
- [x] Golden audit output.

Acceptance:

```bash
cargo test -p monad-mev-engine simulated_execution_template
```

### WP-30: Risk-Checked Submit Path

Status: [x]

Goal: wire generic transaction candidates through simulation, risk, nonce, and
explicit submission.

Implementation tasks:

- [x] Add composite executor stage chain.
- [x] Require simulation result.
- [x] Require risk approval.
- [x] Require explicit production config.
- [x] Add submit dry-run mode.
- [x] Add audit log for each stage.

Testing tasks:

- [x] Default mode records only.
- [x] Dry-run mode validates only.
- [x] Missing risk rejects.
- [x] Fake submit success.
- [x] Fake submit failure audit.

Acceptance:

```bash
cargo test -p monad-mev-exec risk_checked_submit
```

### WP-31: Metrics And Tracing

Status: [x]

Goal: expose operational visibility for live and production runs.

Implementation tasks:

- [x] Define metric names.
- [x] Add ingestion latency metrics.
- [x] Add strategy latency metrics.
- [x] Add simulation latency metrics.
- [x] Add risk rejection counters.
- [x] Add execution receipt counters.
- [x] Add channel depth/dropped item metrics.
- [x] Add structured tracing spans.

Testing tasks:

- [x] Metric registration test.
- [x] Counter increment test.
- [x] Trace span smoke test.
- [x] No-cardinality-explosion review test where practical.

Acceptance:

```bash
cargo test --workspace metrics
```

### WP-32: Persistent State Backend

Status: [x]

Goal: add optional durable state for long-running strategies.

Implementation tasks:

- [x] Choose first backend: SQLite or RocksDB.
- [x] Add feature-gated backend.
- [x] Persist protocol state snapshots.
- [x] Persist audit records or index.
- [x] Add restore path.
- [x] Add migration/versioning model.

Testing tasks:

- [x] Persist/restore test.
- [x] Version mismatch test.
- [x] Crash-like partial write test.
- [x] Feature-gated compile test.

Acceptance:

```bash
cargo test -p monad-mev-store --features persistent
```

### WP-33: Snapshot/Live Parity Test Suite

Status: [x]

Goal: prove strategy behavior is consistent between replay and live semantic
paths.

Implementation tasks:

- [x] Add parity fixture format.
- [x] Add captured stream golden.
- [x] Add strategy output golden.
- [x] Add timing parity assertions.
- [x] Add live ignored test.

Testing tasks:

- [x] Fixture parity test.
- [x] Snapshot parity test.
- [x] Live ignored parity test.
- [x] Golden strategy output.

Acceptance:

```bash
cargo test --workspace parity
MONAD_MEV_EVENT_RING=monad-exec-events cargo test --workspace parity -- --ignored
```

### WP-34: Production Runbook

Status: [x]

Goal: document how to run safely from replay to production.

Implementation tasks:

- [x] Update getting-started docs for v0.2.
- [x] Add protocol capture guide.
- [x] Add strategy package guide.
- [x] Add simulation guide.
- [x] Add risk config guide.
- [x] Add production executor guide.
- [x] Add incident response guide.

Testing tasks:

- [x] README smoke commands.
- [x] Docs command smoke tests where practical.
- [x] Link check where practical.

Acceptance:

```bash
cargo test --doc --workspace --all-features
```

### WP-35: Release Hardening

Status: [x]

Goal: prepare v0.2 for use by external developers.

Implementation tasks:

- [x] Review public API names.
- [x] Review feature flags.
- [x] Review error messages.
- [x] Review unsafe/production gates.
- [x] Review docs for platform claims.
- [x] Update changelog.
- [x] Update release checklist.
- [x] Run full CI locally.

Testing tasks:

- [x] Full workspace tests.
- [x] Full all-feature clippy.
- [x] Doctests.
- [x] Generated strategy package tests.
- [x] Fake production executor tests.

Acceptance:

```bash
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --workspace --all-features
cargo test --doc --workspace --all-features
```

## 6. Testing Strategy

### 6.1 Always-On Tests

These should run in CI:

- unit tests
- fixture tests
- golden JSON/JSONL tests
- adapter/state contract tests
- engine routing tests
- state rollback tests
- risk policy tests
- fake executor tests
- doctests

### 6.2 Feature-Gated Tests

These run in CI when dependencies are available:

- Monad EVM simulation backend tests
- persistent backend tests
- RPC submit fake transport tests

### 6.3 Ignored Integration Tests

These require external infrastructure:

- real live event ring
- real snapshot corpus
- real RPC state hydration endpoint
- real production submit smoke test on a safe devnet account, if ever allowed

Ignored tests must be documented and must never require private keys in source.

## 7. CI Plan

v0.2 CI should include:

- format
- clippy all targets/all features
- workspace tests all features
- doctests
- coverage
- feature matrix for `live`, `persistent`, `monad-evm`, and executor features
- generated strategy package smoke test

Production executor tests must run with fake transports by default.

## 8. Documentation Plan

Required v0.2 docs:

- `docs/v0.2/getting-started-protocol-capture.md`
- `docs/v0.2/strategy-packages.md`
- `docs/v0.2/engine.md`
- `docs/v0.2/state-stores.md`
- `docs/v0.2/lifecycle-harness.md`
- `docs/v0.2/simulation.md`
- `docs/v0.2/risk.md`
- `docs/v0.2/execution.md`
- `docs/v0.2/production-runbook.md`

Docs must keep the mode boundaries clear:

- replay
- live observe
- recording
- dry-run
- simulation
- production submission

## 9. Resolved Decisions

These decisions are resolved for the current v0.2 implementation:

- Use synchronous traits for the core API and add async wrappers later if needed.
- Use simulation contracts plus fake/backend adapters; pinning a concrete Monad
  EVM backend remains a feature-gated integration decision.
- Use JSON file snapshots as the first persistent backend.
- Use fake transports for always-on executor tests and keep production transport
  interfaces explicit.
- Keep crates Git-only until production executor APIs stabilize.
- Use explicit config flags for production submission.

## 10. Initial v0.2 MVP

The recommended MVP is WPs 1-16:

```text
Execution Events stream
-> protocol capture filters
-> strategy package conventions
-> engine runtime
-> in-memory state
-> protocol adapters
-> read-only lifecycle detector
-> fixtures and goldens
```

This MVP proves the developer experience:

```text
capture events for protocol X -> attach strategy Y -> produce deterministic
opportunity records
```

Simulation, risk, and production execution should start only after this path is
clear and tested.
